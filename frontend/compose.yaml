services:
  backend:
    build: ./backend
    container_name: ask-my-notes-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: ask-my-notes-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend